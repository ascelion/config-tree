
dependencyManagement {
	imports {
		mavenBom 'ascelion.shared:cdi-utils-bom:2.0.0-SNAPSHOT'
	}

	project.withConfiguration( 'testCompile' ) {
		configurations( it ) {
			imports {
				mavenBom "org.apache.deltaspike.distribution:distributions-bom:${versions.deltaspike}"
				mavenBom "ascelion.shared:cdi-unit:2.0.0-SNAPSHOT"
			}
		}
	}
}
